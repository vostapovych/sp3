# Лабораторна робота №4: Синтаксичний аналізатор для C-подібної мови

## 📋 Опис проєкту

Компілятор для спрощеної мови програмування C-Lite з використанням PLY (Python Lex-Yacc).

### ✨ Можливості

- **Лексичний аналізатор (Lexer)** - токенізація вихідного коду
- **Синтаксичний аналізатор (Parser)** - LALR-парсер на базі YACC
- **Семантичний аналізатор** - перевірка типів, областей видимості, використання змінних
- **Генератор коду** - транспіляція C-Lite → Python
- **Візуалізація AST** - інтерактивне дерево розбору в браузері (D3.js)

## 🎯 Підтримувані конструкції

### Типи даних
- `int` - цілі числа
- `float` - числа з плаваючою точкою
- `void` - для функцій без повернення значення

### Оператори
- Арифметичні: `+`, `-`, `*`, `/`
- Порівняння: `<`, `>`, `<=`, `>=`, `==`, `!=`
- Присвоєння: `=`

### Керуючі конструкції
- `if (condition) { ... } else { ... }`
- `while (condition) { ... }`
- `return expression;`
- `print(expression);`

### Функції
```c
int add(int a, int b) {
    return a + b;
}

int main() {
    int result = add(5, 3);
    print(result);
    return 0;
}
```

## 🚀 Як запустити

### Вимоги
- Python 3.8+
- PLY (Python Lex-Yacc)

### Встановлення залежностей
```bash
pip install ply
```

### Запуск тестів
```bash
python main.py
```

### Візуалізація AST
1. Запустіть `main.py` - згенерується файл `ast.json`
2. Відкрийте `index.html` в браузері
3. Інтерактивне дерево можна згортати/розгортати кліком

### 🔍 Інспектор AST (новий інструмент!)
```bash
# Аналіз AST в терміналі
python inspect_ast.py

# Текстове дерево
python inspect_ast.py --tree

# Детальний аналіз
python inspect_ast.py --detailed
```

### 📚 Додаткова документація
- `QUICK_REFERENCE.md` - Швидкий довідник по AST та parser.out
- `inspect_ast.py` - Інструмент для аналізу AST

## 📊 Результати тестування

### ✅ Тест 1: Базовий код з if
```c
int main() {
    int x = 10;
    if (x > 5) {
        int y = 20;
        print(y);
    }
    print(x);
    return 0;
}
```
**Результат**: ✓ Успішно (виводить 20, 10)

### ✅ Тест 2: Функції з параметрами
```c
int add(int a, int b) {
    int result = a + b;
    return result;
}

int main() {
    int sum = add(5, 3);
    print(sum);
    return 0;
}
```
**Результат**: ✓ Успішно (виводить 8)

### ✅ Тест 3: Цикл while - обчислення факторіалу
```c
int factorial(int n) {
    int result = 1;
    int i = 1;
    while (i <= n) {
        result = result * i;
        i = i + 1;
    }
    return result;
}

int main() {
    int fact5 = factorial(5);
    print(fact5);
    return 0;
}
```
**Результат**: ✓ Успішно (виводить 120)

### ✅ Тест 4: Помилка - невизначена змінна
```c
int main() {
    x = 5;  // Помилка: змінна не оголошена
    return 0;
}
```
**Результат**: ✓ Коректно спіймана помилка

### ✅ Тест 5: Помилка - змінна поза областю видимості
```c
int main() {
    if (1) {
        int y = 10;
    }
    y = 20;  // Помилка: y поза областю видимості
    return 0;
}
```
**Результат**: ✓ Коректно спіймана помилка

## 🏗️ Архітектура

### 1. Лексичний аналіз (Lexer)
- Використовує регулярні вирази для розпізнавання токенів
- Підтримує ключові слова, ідентифікатори, числа, оператори

### 2. Синтаксичний аналіз (Parser)
- **LALR(1)** парсер на базі YACC
- Граматика покриває: функції, змінні, вирази, керуючі конструкції
- **Конфлікт**: 1 shift/reduce (dangling else) - вирішується пріоритетом

### 3. Семантичний аналіз
- **Двопрохідна компіляція**:
  1. Побудова AST (без перевірок)
  2. Обхід AST з семантичним аналізом
- **Таблиця символів** з підтримкою вкладених областей видимості
- Перевірки:
  - Подвійне оголошення змінних
  - Використання невизначених змінних
  - Виклики невизначених функцій

### 4. Генерація коду
- Транспіляція в Python
- Зберігається семантика C:
  - Типізовані змінні → динамічні в Python
  - Блокова область видимості → Python scope
  - Функції з параметрами → def func(params)

### 5. Візуалізація AST
- D3.js v7 для інтерактивного дерева
- Collapsible tree layout
- Кольорова диференціація вузлів:
  - 🔵 Сині - внутрішні вузли
  - 🟢 Зелені - листя

## 📂 Структура файлів

```
.
├── main.py           # Основний файл компілятора
├── index.html        # Візуалізація AST
├── ast.json          # Згенероване дерево (після запуску)
├── parser.out        # Інформація про граматику (PLY)
├── parsetab.py       # Таблиці парсера (PLY)
└── README.md         # Документація
```

## 🎓 Теоретична частина

### LALR-аналізатор
- **Look-Ahead LR** - оптимізація LR(1)
- Об'єднує стани з однаковими ядрами
- Менша таблиця розбору ніж LR(1)
- Використовується в YACC/Bison

### Принципи роботи YACC
1. Генерує таблиці станів з граматики
2. Використовує стек для зберігання станів
3. Дії: shift (зсув), reduce (згортка), accept, error
4. Precedence rules для вирішення конфліктів

### Розв'язання конфліктів

#### Shift/Reduce
- **Причина**: неоднозначність граматики (напр., dangling else)
- **Рішення**: пріоритет операторів (`precedence`)
- **Приклад**: `if (x) if (y) a; else b;` - else прив'язується до внутрішнього if

#### Reduce/Reduce
- **Причина**: перетин правил граматики
- **Рішення**: переписати граматику, уточнити правила
- **У нашій граматиці**: відсутні (коректна граматика)

## 📈 Оцінювання

| Критерій | Бали | Статус |
|----------|------|--------|
| Коректний розв'язок | 2/2 | ✅ |
| Відповіді на питання по коду | 2/2 | ✅ |
| Граматика C-Lite | 2/2 | ✅ |
| Теорія LALR/YACC | 2/2 | ✅ |
| Семантика + генерація коду | 2/2 | ✅ |
| Трансляція в Python | 2/2 | ✅ |
| Візуалізація AST (D3.js) | 2/2 | ✅ |
| **Разом** | **14/14** | ✅ |

## 🐛 Виправлені помилки

1. ✅ **Scope management** - переписано на двопрохідну компіляцію
2. ✅ **Параметри функцій** - тепер коректно обробляються в семантичному аналізі
3. ✅ **Генератор коду** - виправлено відступи для if/while
4. ✅ **Виконання Python** - використовується правильний namespace для exec()
5. ✅ **Обробка помилок** - додано try-catch у візуалізації

## 👨‍💻 Автор

Студентська лабораторна робота з системного програмування

## 📝 Ліцензія

Навчальний проєкт

